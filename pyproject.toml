[project]
name = "wan2-1-mac"
version = "0.1.0"
description = ""
authors = [
    {name = "HighDoping",email = "HighDoping@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.10,<4.0"
dependencies = [
    "einops (>=0.8.1,<0.9.0)",
    "torch (>=2.4.0)",
    "torchvision (>=0.19.0)",
    "opencv-python (>=4.9.0.80)",
    "diffusers (>=0.31.0)",
    "transformers (>=4.49.0)",
    "tokenizers (>=0.20.3)",
    "accelerate (>=1.1.1)",
    "tqdm (>=4.67.1,<5.0.0)",
    "imageio (>=2.37.0,<3.0.0)",
    "easydict (>=1.13,<2.0)",
    "ftfy (>=6.3.1,<7.0.0)",
    "dashscope (>=1.22.1,<2.0.0)",
    "imageio-ffmpeg (>=0.6.0,<0.7.0)",
    "gradio (>=5.0.0)",
    "numpy (>=1.23.5,<2)",
    "modelscope (>=1.23.2,<2.0.0)",
    "huggingface-hub[cli] (>=0.29.2,<0.30.0)",
]

[tool.poetry]
packages = [{include = "wan2"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
